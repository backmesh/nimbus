rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /journalists/{userId}/{documents=**} {
      allow read, write: if request.auth != null && userId == request.auth.uid
    }
  }
}